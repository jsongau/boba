# CHANGELOG — Boba Night

One line per working day. Details live in docs/SESSION-*.md files.
- 2026-07-22 (dock polish): lamp toggle now self-describing — label swaps "All hours" (off, dim brass knob) <-> "Open now" (on, jade label + track floods jade + LED); rating/reviews moved from under the miles to directly beneath the shop name ("★ 4.2 · 117 on Google", line always reserved — unrated shops say "No Google rating yet" so the card never shifts); pills got their full width back (no more truncated city). POUR MODES: new two-state control beside the pour button — arrow = closest-in-order (default), pearl-pip dice = surprise; dice mode relabels the button "Surprise pour" and pulls a random shop (never the same one twice in a row). Headless-verified: label/tint state flip, actionsY constant across 8 pours incl unrated card, random indices non-sequential + no immediate repeats.
- 2026-07-22 (404 round): CUSTOM 404 PAGE (root 404.html, Vercel serves it for every dead link automatically; noindex,follow). Luxury retention page: 4-[spinning pearl-cup SVG]-4 numeral, "This page poured its last cup." host-voice hero, working city/ZIP search bar + Tonight city chips that open the REAL finder dock right on the 404 (hand-off to the nav engine via __demo.pick), Open-the-boba-map + open-late CTAs, framed art slot at /img/404-teahouse.png (elegant SVG placeholder + auto-swap when the generated image lands), 5-chapter tea→boba history timeline (Shennong legend labeled as legend, Lu Yu's Cha Jing, the disputed 1980s Taiwan first-cup claim stated as disputed, 1990s SGV arrival, tonight's 998/169), keep-exploring link row, universal nav+footer via components. COMPONENT FIX uncovered by this build: components/nav.js now loads the full search chain (nav-midnight -> finder-data -> finder -> near-me -> sound) and components/nav.html carries the results dock markup — the nav search bar was dead on component-driven pages before this. Headless-verified desktop+mobile: zero JS errors, chip tap -> "Pouring near Cypress · 1 of 1 in Cypress", image fallback renders.
- 2026-07-22 (dock round): NEAR-ME DOCK UX + LIVE DATA. City lock: picking a city or tapping the GPS pin locks a champagne chip in the nav search bar (pin + city + padlock; tap to edit, re-locks on blur); GPS now resolves coords to the nearest SoCal city so the dock reads "Pouring near Rowland Heights" instead of "your location". City-scope toggle: second Velvet Channel segmented [City N | Within X mi M] defaults to the locked city, widening pulls in neighbors; out-of-city shops light their city pill champagne; footer counter switches "in {city}" / "within X mi". Card stabilized: name reserves 2 lines, hours line always renders, pills capped at exactly 2 ([city|type] + status), one line, ellipsized. Luxury toggles: "Lamp Crown" Open-now switch (brass knob SVG, jade LED ignites 120ms after the knob lands, 4-layer glow) + "Velvet Channel" sliding-indicator segmented for All/Originals/Chains (transform-only, reduced-motion safe). LIVE RATINGS: finder.js fetches rating+reviews from niteboba_finder at load (session-cached) and renders "★ 4.6 / 163 on Google" under the miles — never baked, never stale. HOURS FIX: the "Today:" line is now derived client-side from the baked open periods (the July 20 finder-data regen had dropped the pre-formatted weekly text, which made every card say "Hours being verified" — data was never stale, only the display string was missing); shops with no periods today say "Closed today". Mobile hero: map CTA card relocated below the two hero cards under 1080px (bmorder script). All headless-verified at 390px and 1280px. Flag: 14 of 890 open shops lack a Google rating in Supabase.
- 2026-07-22 (nav round): UNIVERSAL MEGA NAV shipped: components/nav.html (full header markup: wordmark, 5 mega panels, search minibar w/ animated pin beacon, miles meter, drawer, mobile bottom bar) + components/nav.js (2-line template contract: #bn-nav div + one script tag; attaches nav-midnight/finder/sound css, loads nav-midnight.js -> near-me.js -> sound.js in order; baked-header pages auto-skipped so including it everywhere is safe). Contract doc: docs/universal-nav.md. GEO BRIDGE: window.__bnApplyGeo exposed by below-hero.js finder (800ms double-apply guard) and called from the geolocation interceptor in nav-midnight.js — the nav beacon (or any successful location fix) now fills the finder city, sets "Near {city}, {region}", and personalizes Open Near You / directory chips in one tap; headless-verified (bare-page inject + panel open + pinned bottombar at 390px + geo apply-once). PREVIEW SUPPORT (same day, after a template prompt baked its own stale nav copy into a Downloads preview): nav.js now reads its own script URL and, when loaded cross-origin or from file://, pulls markup/css/behavior js from that origin and rewrites nav links to absolute — previews load the LIVE nav via one absolute script tag, never a baked copy; vercel.json adds Access-Control-Allow-Origin on /components/* to allow the cross-origin markup fetch; universal-nav.md + universal-footer.md gain a previews-and-off-site section; chrome-kit README updated. Verified headless from file:// against a CORS-stubbed origin.
- 2026-07-22: HOMEPAGE BELOW-HERO REDESIGN shipped (production splice of 19 preview rounds). index.html below-hero replaced with 13 scoped sections under #bh (css/below-hero.css + js/below-hero.js): location finder w/ typeahead+GPS, Open Near You with vibe+region filter chips, Society Directory (live counts, spelled headline), sushi-conveyor Gallery (Trending + Worth the Drive dual auto-belts, omakase chains toggle default off), City After Dark, Concierge, House Edit, dark Drink Room w/ custom SVG icon sprite (8 drink + 6 vibe + 5 utility icons, 24-grid champagne/pink system), After Ten, Roll the Night, Social Calendar w/ calendar CTA card, House Guide, Keep Exploring one-line pill rail. Hero: search form + proof line removed, animated map-card CTA added; static counts updated 334/46 -> 998/169 (meta+schema too) with client-side live refresh from niteboba_finder. Search floater gated (hidden over hero, session-only dismiss; duplicate ty-float/dock block removed - was a live bug); bulk floater now "Boba for the whole crew" / Tastea Partea Jugs only. Footer rebuilt (.ft2: crest, live stat row, open-late pill, 4 nav columns incl all 6 regions). Truth-gate: Mung Bean Milk Tea restored to 7 Leaves Garden Grove (Tastea repoint was unverified). New docs/copy-voice.md (researched engagement-language guide, also installed as boba-night-copy-voice skill). Copy pass v2 (same day): metaphor budget enforced (House/Pour/Night only), headings rewritten query-shaped for SEO/GEO (What SoCal is drinking right now / Open late / Boba events this week / The best boba neighborhoods after dark), 'Order this first' pick labels, ledes carry liftable fact statements. boba-night-copy-voice skill updated with the budget + no-visual-dependent-headline + search-shaped-heading rules. Same-day v3: chains toggle now global and synced across Open Right Now, the conveyor belts, and Open Late (default: independents only; labels 'Independents only' / 'Chains included', sushi vocabulary removed); 'Partea Jugs' trademark removed from floater copy (link label now 'Order at Tastea'). Mobile round (same day): stacked two-line wordmark frees width for a longer nav search pill; nav location-pin button unburied (input z-index was painting over it) with custom three-state animated SVG (idle pin / radar-ring search / locked-in check), same states on the finder's Find-boba-near-me button via a geolocation interceptor in nav-midnight.js; bottom quick bar now permanently pinned (hide-on-scroll disabled); share card og-card.png + og/twitter tags + query-shaped title; mobile sound toggle unhidden (was display:none under 1080px). Evening round: chains toggle + CTA moved under the lede (left) on the conveyor and Open Late sections; sparkle-pearl LED SVG replaces the flat pink dots on both floaters; Party & Event floater now waits until the reader scrolls past the conveyor. Copy v4 (external audit triage; 8 of 12 findings were stale, already shipped): directory H2 now 'Find boba shops near you.' (count moved to lede as citable fact), 'Late-night boba.' heading, 'Boba guides' kick, footer Society column renamed Community, last 'rooms' references purged, kicks keep franchise names while H2s stay brand-free. Hero unchanged deliberately (tonight = positioning). Universal footer shipped: components/footer.css + components/footer.js (self-contained, live counts w/ session cache); any template includes 3 lines (link, #bn-footer div, script) and footer edits propagate site-wide without regenerating pages; homepage keeps baked copy for SEO, component replaces it at load; footer CSS removed from below-hero.css (single source). Contract doc: docs/universal-footer.md. Details: docs/SESSION-2026-07-22-below-hero.md.
- 2026-07-20 (pm3): DATE NIGHT HUB rebuilt. /best/date-night/ goes from a "Verifying" shell to an interactive, truth-gated hub reading 622 Supabase shops: emotional hero + GEO answer lede naming tonight's top picks, a mood finder (Date-worthy/First date/After dinner/Open late/Weekend late/Near me) over 234 date-worthy independents, ranked cards with attributed Google ratings + tonight's hours, five "near where you're eating" landmark collections (Disneyland/Irvine Spectrum/South Coast Plaza/Little Saigon/Old Town Pasadena) computed from coords via haversine, a self-contained SVG locator map (no external map dep), a Build-a-date itinerary, open-late + under-the-radar cuts, a drink-guide rail, and CollectionPage+ItemList+FAQPage+BreadcrumbList schema. Flipped noindex->index,follow (234 shops + method shown clears the bar). Truth-gated: no seating/ambiance data exists yet so Quiet/Outdoor/Cozy render as honest "Verifying" cards; dropped Luxury/Instagram-Worthy/Trending (no data); "hidden gem" (banned) -> "Under the radar"; ratings shown "N on Google", dated; hanzi names get pinyin + zh-CN pronounce. Links coverage-aware: the 56 date-worthy shops with committed profiles link to them, the other 178 link to their city page (zero 404s) until the 407 profiles land. Bespoke page (not gen_site): inline page CSS/JS + baked dn-data script. Details: docs/SESSION-2026-07-20-date-night.md.
- 2026-07-20 (open-late): /best/open-late/ rebuilt below the hero into a live, Supabase-driven late-night discovery page (open-now in Pacific, 8 PM to 2 AM time slider, Leaflet night map, hidden gems, shuffle, build-a-crawl, save/black-book). Owned by new build/gen_open_late.py; gen_site now skips the slug. Static crawlable shell (171 internal links; CollectionPage+ItemList+BreadcrumbList+FAQPage schema, speakable) + live niteboba_finder layer. Data recalibrated to reality (450 open past 9 PM, 40 past midnight; not a 2 AM market). Drinks = educational links (needs categories backend); trending omitted (needs telemetry). New css/open-late.css, js/open-late.js, build/open-late-data.json. Details: docs/SESSION-2026-07-20-open-late.md.
- 2026-07-20 (pm): FAVICON shipped. Flat SVG twin of the nav boba-cup logo (V1: cup + tapioca pearls + champagne crescent moon + flamingo ripple) on the obsidian #0B0C0E badge - new root favicon.svg + favicon.ico (16/32/48) + apple-touch-icon.png (180, full-bleed). Baked into all four generator heads (gen_site/gen_profiles/add_spots/gen_events) and stamped across 973 existing pages via new build/apply_favicon.py (idempotent; re-run after regen). Site had no favicon before. Details: docs/SESSION-2026-07-20-favicon.md.
- 2026-07-20: SUPABASE UNIFICATION + expansion. Profile + scaffold generators now read the Supabase niteboba table (single source of truth), replacing the stale seed CSV. Generated 407 net-new profile pages (all open+enriched shops that lacked one) and rebuilt the full scaffold from Supabase: 167 city pages (was 48), 6 region hubs incl new Ventura County / 805 hub (28 shops, 8 cities), 174-URL sitemap. Wave-2 enrichment (Google rating/reviews, price, service/parking/amenities) on profiles; hero rating, stacked linkable Area rows, ZIP-shared-city links. 0 broken internal links (all 622 city-page links resolve). Flags: 119 stale profile pages = the 99 closed + 12 seed + 8 temp-closed shops (delisted from city pages, pending noindex/remove); mega-nav region rail still derives from directory/index.html SHOPS (334/46 counts, no Ventura) until nav_data is repointed at Supabase — next task.
- 2026-07-17: MIDNIGHT WAVE (multi-agent build). Unified nav rolled to all 438 pages (6-item mega-menu: Shops/Best for/New/Pick for me/Guides/Ratings & meetups + cmd-K search + mobile bottom bar; one source build/nav_data.py). Homepage rebuilt 14->9 sections, 12160px->6654px, new identity hero. Anti-Yelp profile generator (build/gen_profiles.py): verification ledger, live open-now from baked hours, dark Leaflet map, provenance stamps, nearby rails, closed-shop epitaph variant; 3 samples live. Events system (Series/Occurrence, build/gen_events.py): restyled /meetups/, cadence chips, ICS feeds, per-shop badges, submit flow. Full enriched warehouse exported to data/stores-data.json (334 rows, 213 hours, 322 coords). Fixed directory dead old-nav JS crash post-rollout.

- 2026-07-17: bobanight.com live (Porkbun DNS → Vercel); rebrand NiteBoba → Boba Night sitewide (446 files, canonical → https://www.bobanight.com); new build/sync_from_supabase.py — DB is source of truth; 99 closed shops chipped + pulled from roulette, 215 verified-open chips. PM: directory re-skinned to obsidian midnight theme (boutique/chains sectioned, editorial filters, typographic cards, film grain, one-neon budget); purple now Featured-pill-only. PM2: six-agent product audit -> docs/PLAN-MIDNIGHT-WAVE-17JUL26.md approved; P0 hotfixes shipped (invisible solid CTAs, nav count 334 incl &middot; form, banned-word chip, JS-gated reveals replaced with CSS scroll-driven animation - 34/34 sections visible without JS, stale directory fallback count, archive/ pages removed).

- 2026-07-18: AFTER DARK palette rolled to ALL pages. Purple reintroduced as a ROYAL accent (orchid #b46bd6 / imperial #5c2c86 glow + medallion halo), not the old lavender wash; velvet-oxblood + neon-pink + jade radial glow added to every body via shared tokens (css/site.css, luxury-homepage.css, nav-midnight.css, motif.css + build/gen_profiles.py). Regenerated 333 profiles + 46 city + 5 region + 13 intent pages. Neon action colour bumped #ff3f6f -> #ff2f6d. Featured slot to move to gilt-gold since purple is now decorative (flagged, not yet changed). Aesthetic direction for gifting/dice-roll = "After Dark" (everyday date-night wording, luxe/neon look); direction previews after-hours-preview-v2/v3 kept OUT of repo.

- 2026-07-19: NEARBY FINDER shipped as standalone page /nearby/ (zero risk to the 438 existing pages). Pearl Line single-row header: brand + Shops/Best for/New/Pick for me mega-dropdowns, cream City/ZIP field, collapsible cherry miles meter (drag the pearl 1–20mi, chip label syncs), Closest/Open sort, sound, Guides + Ratings on the right, Tonight CTA. Taro Yuan floater doubles as universal search (tea houses / pages / ingredients). Production copy + JSON-LD (WebSite+SearchAction, BreadcrumbList), canonical https://www.bobanight.com/nearby/, index,follow, 5 real /area/ region links. Inlined DATA (322 shops) untouched (md5 bc29f344d082 stable). Headless-verified: no JS errors, meter drags 5→20→5, floater search returns hits. Data warehouse for the 77 empty SoCal cities (scrape_boba.py) still pending user's Places key.

- 2026-07-19 (pm): SIGN-IN PORTAL + accounts. Replaced the /nearby/ CTA (Mystery Roll, built then cut) with "Sign in" -> new /account/ portal. Supabase Auth via email one-time codes (no password, no redirect config); page self-heals if the JS lib is slow (3.5s fallback + guarded init). Signed-in profile shows synced saved spots; the localStorage Black Book auto-migrates into the account on first sign-in. Backend in the shared BOBA Supabase project (uqefyfqwwkkvydkgepgf), namespaced: boba_profiles + boba_favorites, owner-only RLS. SECURITY: closed the anon-write hole on 6 CoverCapy reference tables (dental_carriers/plans/premiums, dhmo_providers, dental_procedure_codes, worker_eligibility) — enabled RLS + public-read policy (service key still writes). Also rebuilt the Taro Yuan floater to lead with search (magnifier + prompt) instead of the featured-profile CTA. Sitewide header rollout still pending (shared-asset extraction approved, not yet run).

- 2026-07-19 (pm2): MEGA NAV SITEWIDE. Rolled the Pearl Line finder header to all 440 pages via shared cached assets — css/finder.css, js/finder.js (engine+floater), js/finder-data.js (322 shops, trimmed, downloaded once). New nav partial (templates/nav.html) adds the City/ZIP finder, miles meter, Closest/Open, sound, and Sign in (-> /account/) to the existing mega dropdowns; Taro Yuan floater = site search. build/apply_finder_nav.py swaps the old nav block + injects asset links (dry-run default; skips /nearby/, /account/, and 7 nav-less pages). Header verified overflow-free 1080-1600 on homepage + listing + city archetypes; overflow-x:clip safety net. Existing page bodies untouched.

- 2026-07-19 (pm3): NAV V3 "NEON CUP" sitewide (picked from 3 verified variants). Dropdowns now open directly under their own trigger (JS-clamped to viewport) instead of hugging the left edge. Rigid chevron gone: hover draws a champagne underline from center, lit neon while open. "New" tab removed (its links live on as a "New openings" rail link in Shops); "Ratings & meetups" renamed "Society"; freed space grew the City/ZIP field ~50%. "5 mi" chip now opens a floating Set-your-range popover: an SVG boba cup that fills with milk tea as the radius grows (drag vertically, scroll wheel, or -/+ steppers; chip label live-syncs) replacing the old expand-the-navbar meter. Mobile: search field sits in the bar as a pill next to the brand, outside the hamburger. Shipped via shared assets (css/finder.css, js/finder.js) + build/update_nav_v3.py markup pass over 443 pages + templates/nav.html + build/nav-finder-partial.html (idempotent, dry-run default). Verified headless: panel centered 0px off trigger, cup renders, +/- steps 5->7 with chip sync, pill search on 390px, zero JS errors, zero overflow 1080-1600.

- 2026-07-19 (pm4): Nav finalized as V1 "PEARL RISE" (user pick over V3) + finder UX fixes. Pearl caret (rises/glows on hover, neon when open), jewel-box radius popover keeping the cream drag-track with ticks. Radius changes are now SILENT (engine holds its search while the popover is open — no more results card popping mid-adjust; +/－, drag, wheel, keys all silent) and a "Set range" button closes the popover and pours once. Results card gains a filter row: Show All / Originals (n) / Chains (n) / Open now — independents are "Originals" (card tag renamed Specialty -> Original). Closest/Open sort toggle removed from the bar (results always pour nearest-first; open-now moved to the card as a filter; "most reviews" needs ratings data we don't store yet). Search field grew again with the freed space. finder-data.js regenerated with ty/f/ad — fixes every card reading "Chain" and broken Directions addresses. No markup pass needed (popover shell is skin-agnostic). Verified: adjust 5->8 with card closed, Set pours once (9->19 shops), Originals->3 / Chains->17 with live counts, zero JS errors.

- 2026-07-19 (pm5): Finder "BEACON" polish sitewide (picked from 3 agent-built options). Empty search field runs a neon border sweep with a bolder prompt; a pulsing pin beacon pours boba near you via geolocation; focused-empty field shows a helper strip led by a glowing crystal-boba orb (SVG, not a dot): "Boba near you in one tap, or type a city." Results card auto-closes when you focus/type the search or open the radius popover; "Up next" line removed; Open now is now a grey-to-green LED pill in the card header (still filters via the same button); suggestion dropdown sized to content with count pills that never clip, crystal-boba header marker, hover slide + gilt rail; picked location renders as an in-field cream chip (pin, city, ZIP capsule, working clear ✕). Default radius now 8 miles. Shipped via shared css/finder.css + js/finder.js only (engine default radius:5 -> 8; no markup pass needed). Verified headless: 8mi default chip, helper wording dash-free, card 1-of-36 within 8mi, LED pill inside header, auto-close on focus, zero JS errors.

- 2026-07-19 (pm6): Meter position fix + tasty crystal boba. The radius handle was computed while the popover was hidden (zero-width track) so it parked at the far left; the engine now exposes updateUI and the popover recomputes on open — 8 mi lands at its true spot on the track. Crystal boba upgraded from a static orb to a 3-frame SVG squash-and-stretch bounce (stretch rising, round glow-pop at the peak, squash landing) on the helper strip and dropdown header; reduced-motion safe. Shared css/js only.

- 2026-07-19 (pm7): Removed every mention of How we rank and How we make money sitewide (445 pages + templates + the /nearby/ search index): nav column links, footer About links, homepage proof-strip link, and the Society dropdown's featured card (now The Pearl Ratings -> /critic/). The two pages are retired from the repo; vercel.json 301-redirects both URLs to the homepage so old links and search results land safely. Also: hovering any nav item now closes the radius popover (silently, keeps the chosen radius, no card pour) via js/finder.js.

- 2026-07-19 (pm8): SEO/GEO indexation pass, research-grounded (Google LocalBusiness spec + Google's AI-features guide: pages must be indexed to appear in AI answers; no special AI files needed — fundamentals win). sitemap.xml regenerated by new build/gen_sitemap.py: 52 -> 285 URLs, now covering all 212 verified shop profiles + 46 cities + 5 regions + guide(7)/tools(6)/pantry/critic/meetups/nearby/directory/cities/about, with lastmod, excluding every noindex page (best/, new/, 122 unverified profiles, /account/). Run it after any page-generating build. build/add_phones.py audited telephone in profile JSON-LD: 299/334 already carry it (post-line-796 regen), 34 have no phone in the warehouse (left honest), 1 profile flagged bad JSON-LD. Schema stack confirmed per Google spec: CafeOrCoffeeShop + geo + PostalAddress + openingHoursSpecification + url + servesCuisine + telephone. Biggest remaining SEO lever: the 122 noindex profiles + 77 empty cities = the Places scrape.

- 2026-07-19 (pm9): /near-me/ ships — the full-screen Boba Night map. All 215 verified shops with live open-now (computed from hours in Pacific time), distance-ranked rail, radius search that drags (move the pink pin, stretch the gold handle), rating/review/park/dog-park filters with live counts, and a right-side shop drawer with Overview, Stroll, and Hours tabs. Stroll lists the nearest dog park and park with walk minutes and walking routes, from map_anchors plus 25 seeded central-LA and SD parks (coords need a Places verification pass). Reviews link out to each shop's Google reviews page (place-id, nothing stored). Every card and drawer links into the static /boba/ca/ profile and city pages. Homepage hero gains "Open the boba map". Sitemap regenerated (near-me picked up automatically).

- 2026-07-19 (pm10): /near-me/ joins the site chrome: the canonical mega nav (templates/nav.html) and the sitewide footer now wrap the map. App styles namespaced (--nm-*) so site and app tokens cannot restyle each other; the map search input renamed to mapq because the nav owns #q; the nav floating search card is hidden on this page since the page itself is search; the app shell sizes itself under the nav at runtime. Map behavior unchanged; 21 headless checks pass.

- 2026-07-20: LOGO in the mega nav. New brand mark (img/logo-nav.png, 31KB, trimmed from the 1024px gen) left of the Boba Night wordmark in the bar + mobile drawer; source = build/nav-finder-partial.html + css/nav-midnight.css, re-stamped to all 439 pages via apply_finder_nav.py. Mobile (<1080px) top-bar Sign in CTA hidden — field + burger get the room; bottom-bar Sign in kept. Headless-verified desktop 1440 + mobile 390, zero pageerrors.
- 2026-07-20: TARO YUAN PILOT fixes. Nav was colliding on profile pages only: profile-v4.css overrode .finder-inline (max-width 330/flex) and the page was missing css/motif.css + css/sound.css (+ js/motif.js, js/sound.js) that the nav needs to style the sound toggle. Removed the override, added the full nav asset bundle to the generator so profile pages render the identical universal nav as the homepage. Replaced the scroll-in order capsule + mobile-only bar with a PERSISTENT sticky order rail (Uber Eats + DoorDash search deep-links w/ custom lacquer art + Directions), desktop and mobile, rel=nofollow sponsored. Verified 320/390/768/1512 no overflow, no pageerrors.
- 2026-07-20: TARO YUAN PILOT (v4 PASSALONG profile). One-page deploy of the new profile template: Pearl Orbit medallion hero, verified hours + live open pill, corridor cards, share-crawl invite modal, order-to-go sheet (Uber Eats + DoorDash search links, custom lacquer art), Find It = real CARTO night-map tiles with pearl markers, tap-through to /near-me/?shop= deep link (new: centers + selects + cup-zoom 15.2, coords fallback + toast for shops not yet in the night-map dataset). Shared assets css/profile-v4.css + js/profile-v4.js. Pearl links to scraped-shop pages 404 until the 312-page full run.

- 2026-07-19 (pm11): /near-me/ filter console + the search lens. Seven scattered filter controls became one hairline-ruled instrument bar (Range · Hours · Shops · Parks & more) with a grouped Filters panel, live facet counts, count badge, and Clear all filters — the winning concept from a four-design, three-judge agent panel, with the judges' fixes applied. The radius circle became a fixed lens: drag the map and the world slides under the pearl-strand ring, the search re-runs automatically for whatever is inside, everything outside is dimmed, and nothing renders beyond the circle. Gilt mile-pearl drag handle resizes 1-20 mi; presets are now 2/5/10/20 (All retired); Search-this-area button removed. 27 headless checks pass.
